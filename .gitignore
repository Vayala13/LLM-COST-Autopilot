.env
.venv/
__pycache__/
*.local.md

# Generated feature matrix — regenerate with `python -m scripts.inspect_dataset`
data/prompt_features.json

# Runtime verifier failure log (Phase 3.2) — prompt hashes only, still local
data/routing_failures.jsonl

# Runtime escalation log (Phase 3.3) — prompt hashes + metrics only
data/escalations.jsonl

# Classifier feedback from routing failures (Phase 3.4) — labeled prompts, local
data/feedback_prompts.jsonl

# SQLite request audit trail (Phase 4.1) — prompt hashes + metrics only, local
data/requests.db
data/requests.db-*

# Phase 6.1 load-test audit DB (heavy; summary lives in reports/)
data/load_test_requests.db
data/load_test_requests.db-*

# Phase 5.2 smoke: temp routing maps under configs/ (never commit)
configs/_smoke_*.yaml
configs/*.yaml.tmp
configs/*.yml.tmp
